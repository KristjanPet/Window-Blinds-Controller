# Onshape reference inputs

Place the complete assembled STEP export here as `reference_assembly.step`.
See the parent README for the export procedure. Add labelled views and any
dimensioned sketches needed to interpret the fixed geometry.

Reference models are inputs. Preserve their original coordinate system and
record their source Onshape version when they are added. No reference geometry
is included in this starter.
